import { useState } from "react";
import { toast } from "react-toastify";
import { PaymentFlow } from "./PaymentFlow";
import axios from "axios";

export const Home = () => {
  return <div>view videos</div>;
};
